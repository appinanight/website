
{% for block in blocks %}

    {% set prevType = block.prev ? block.prev.type : null %}
    {% set nextType = block.next ? block.next.type : null %}
    
    {% include '@snippets/content-blocks/_' ~ block.type ~ '.twig' with { block, prevType, nextType } %}

{% endfor %}
